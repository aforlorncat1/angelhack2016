<%= form_for(@rank) do |f| %>
  <% if @rank.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rank.errors.count, "error") %> prohibited this rank from being saved:</h2>

      <ul>
      <% @rank.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom_id %><br>
    <%= f.text_field :nom_id %>
  </div>
  <div class="field">
    <%= f.label :leaderboard_id %><br>
    <%= f.text_field :leaderboard_id %>
  </div>
  <div class="field">
    <%= f.label :votecount %><br>
    <%= f.number_field :votecount %>
  </div>
  <div class="field">
    <%= f.label :position %><br>
    <%= f.number_field :position %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
